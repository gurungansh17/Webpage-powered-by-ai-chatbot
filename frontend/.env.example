# Backend API URL — use /api if running via Vite proxy, or full URL for production
VITE_API_URL=http://localhost:5000/api

# Google reCAPTCHA v2 SITE key (public key — safe to expose in frontend)
# Get from https://www.google.com/recaptcha/admin
# Key below is Google's test key (always passes) — replace with your real one
VITE_RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI
